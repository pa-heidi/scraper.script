{
  "scripts": {
    "generate-plan": "ts-node generate-plan.ts",
    "quick-test": "ts-node quick-test.ts",
    "plan:interactive": "ts-node generate-plan.ts",
    "plan:test": "ts-node quick-test.ts",
    "plan:help": "ts-node quick-test.ts --help"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}